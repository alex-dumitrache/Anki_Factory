---
description: Re-sync to the constitution — read CLAUDE.md + lessons FRESH from disk and audit the live trajectory for drift. Type any turn to stay on course.
argument-hint: (optional) what we're about to do
---

# COMPASS — Protocol Re-Sync & Drift Check

STOP. Before anything else this turn, re-anchor to the constitution. Do NOT trust the copy in your context — long/compacted sessions AND other sessions modify these files on disk, so what you "remember" may be stale. This skill exists to beat exactly that.

## Do this now, in order

1. **READ fresh from disk** (never from memory — it may be out of date):
   - `C:\Users\<you>\Desktop\Anki_Factory\CLAUDE.md` — the full protocol (Authority, my Blind Spots, the Constitution, the Operating Loop / routing rules, Pre-Edit Discipline, Version Control).
   - `C:\Users\<you>\.claude\projects\C--Users-<you>-Desktop-Anki-Factory\memory\feedback_workflow.md` — every dated cross-session lesson. ⚠️ **Since 2026-07-27 this is mostly a PROMOTION INDEX:** all 20 historical entries are one-line pointers whose rules now live in `CLAUDE.md` §II or a phase skill. Read it for entries that still carry a BODY (those are lessons not yet promoted) and to see where a promoted rule went — but **the substance is now in CLAUDE.md §II Blind Spots, so weight your reading there.**
   Read them IN FULL. If either exceeds one Read call, batch with offset+limit until every line is in context.

2. **Audit the LIVE trajectory** — not just the next file edit, but what we are strategizing / deciding RIGHT NOW (drift happens in conversation, long before any edit). Ask explicitly:
   - Am I about to REINVENT a proven artifact instead of copying its baseline and changing ONE variable?
   - Am I asserting any file's state I have not read or grepped THIS session?
   - Am I designing a fix for a bug I have not confirmed is real (vs. LLM-runtime behavior)?
   - Is this PROMPT-DESIGN work the Operating Loop routes to Chat, not me?
   - Any OTHER Blind Spot, Constitution rule, or dated lesson that bears on what we're about to do?

3. **Report before continuing.** Exactly one of:
   - `ON COURSE` — plus the 1–2 specific rules/lessons you checked against (so it isn't performative), or
   - `DRIFT` — name the exact rule/lesson about to be violated, and the correction.

4. Only then continue with the actual work.

The point is to catch what you are BLIND to, not recite what you remember. Don't manufacture drift — if nothing applies, say "ON COURSE — nothing applicable" and move on.
